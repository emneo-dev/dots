font-family = "JetBrains Mono"

# I need to change that at some point xd
theme = "dark:OneHalfDarkModified,light:OneHalfLight"

cursor-style-blink = false
unfocused-split-opacity = 1

keybind = clear
keybind = ctrl+shift+c=copy_to_clipboard
keybind = ctrl+shift+v=paste_from_clipboard

keybind = ctrl+plus=increase_font_size:1
keybind = ctrl+equal=increase_font_size:1
keybind = ctrl+minus=decrease_font_size:1
keybind = ctrl+zero=reset_font_size
keybind = ctrl+backspace=reset_font_size

keybind = ctrl+shift+l=reload_config
keybind = ctrl+shift+i=inspector:toggle

window-padding-x = 0
window-padding-y = 0
window-padding-balance = true
# Even if I use gnome I lowkey want to still have my topbar in fullscreen
window-decoration = server
gtk-titlebar = false

clipboard-read = allow
clipboard-write = allow
clipboard-trim-trailing-spaces = true

# My tmux already does that, so I don't need that
copy-on-select = false

confirm-close-surface = false
quit-after-last-window-closed = true
resize-overlay = never

# Not supported on Linux, but just in case one day it is
auto-update = off

shell-integration = none
shell-integration-features = no-cursor,sudo,title

window-padding-color = extend

window-padding-x = 3
window-padding-y = 3
